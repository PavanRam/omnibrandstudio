apiVersion: 1

datasources:
- name: Jaeger
  uid: omnibrand-jaeger
  type: jaeger
  access: proxy
  url: http://jaeger:16686
  editable: false
  jsonData:
    # Correlate a Jaeger trace back to Prometheus metrics by service name.
    tracesToMetrics:
      datasourceUid: omnibrand-prometheus
