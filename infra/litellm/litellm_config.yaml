model_list:
# =============================================================================
# LiteLLM model routing — OmniBrand Studio
#
# Layout: FREE TIER first (all Groq → $0), then PAID TIER, then SHARED.
# Tier is chosen by JUDGE_TIER:
#   • campaign graph → backend/pipeline/initial_state.py::resolve_model_aliases
#   • chat layer     → backend/api/routers/conversations.py::_chat_state_context
# FREE aliases must never route to a paid provider. PAID entries below are the
# pre-existing config, relocated into one section unchanged (no model updates).
# =============================================================================

# ============================ FREE TIER — Groq (per-token billing) ===========
# Selected when JUDGE_TIER=free. All deployments are Groq; see model_info for
# per-token prices sourced from https://groq.com/pricing (2026-07-29).

# generation — content_generator / personalization / reflexion ("generation")
# 2026-07-27: groq/moonshotai/kimi-k2-instruct was decommissioned by Groq
# (returned 404 "model does not exist"), which 404'd every reflexion/generation
# call routed to the primary deployment. Promoted the previously-fallback
# llama-3.3-70b-versatile to primary (confirmed live via /v1/models) and added
# gpt-oss-120b as a distinct, high-quality cross-family fallback.
- model_name: "gen-free"
  litellm_params:
    model: "groq/llama-3.3-70b-versatile"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "gen-free-llama-3.3-70b"
    input_cost_per_token: 5.9e-7
    output_cost_per_token: 7.9e-7
- model_name: "gen-free"
  litellm_params:
    model: "groq/openai/gpt-oss-120b"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "gen-free-gpt-oss-120b"
    input_cost_per_token: 1.5e-7
    output_cost_per_token: 6e-7.00000000

# Free judge panel (Groq) — three DISTINCT families for cross-family
# disagreement signal, no paid spend. judges.py keys judge-1/2/3 resolve to
# these via resolve_model_aliases when JUDGE_TIER=free.
#   judge-1-free → GPT-OSS family   (lead)
#   judge-2-free → Llama family     (diversity)
#   judge-3-free → Qwen family      (open-source cross-family check)
# 2026-07-29: judge-3 was groq/qwen/qwen3-32b, which Groq decommissioned (404
# "model does not exist"). Its live successor is groq/qwen/qwen3.6-27b —
# confirmed via /v1/models AND verified to support response_format=json_object
# + temperature=0 when called directly against Groq. (An earlier 400 seen while
# probing this through the litellm proxy was a proxy "invalid model name" error
# because the raw name wasn't a configured model_name — NOT a Qwen JSON
# limitation.) A cross-family llama-3.1-8b-instant fallback keeps the third
# judge alive if Qwen has a single-provider hiccup.
- model_name: "judge-1-free"
  litellm_params:
    model: "groq/openai/gpt-oss-120b"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "judge-1-free-gptoss"
    input_cost_per_token: 1.5e-7
    output_cost_per_token: 6e-7.00000000
# gpt-oss-120b occasionally returns a transient 400 under Groq load; the
# gpt-oss-20b sibling deployment below gives litellm a same-family retry target.
- model_name: "judge-1-free"
  litellm_params:
    model: "groq/openai/gpt-oss-20b"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    input_cost_per_token: 7.5e-8
    output_cost_per_token: 3e-7.00000000

- model_name: "judge-2-free"
  litellm_params:
    model: "groq/llama-3.3-70b-versatile"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "judge-2-free-llama"
    input_cost_per_token: 5.9e-7
    output_cost_per_token: 7.9e-7
- model_name: "judge-2-free"
  litellm_params:
    model: "groq/llama-3.1-8b-instant"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    input_cost_per_token: 5e-8
    output_cost_per_token: 8e-8

# judge-3-free — gpt-oss-20b primary with llama-3.1-8b-instant as secondary.
# 2026-07-29: qwen/qwen3.6-27b was tried and verified to work on SHORT prompts
# (direct Groq test passes json_object), but fails with json_validate_failed
# (failed_generation: "") on the REAL multi-criterion judge prompt — the long
# system template exhausts its JSON generation budget. litellm treats 400 as
# non-retryable (retry_policy.BadRequestError.num_retries=0), so the secondary
# llama deployment was never reached. Switched to gpt-oss-20b (proven reliable
# for the full judge prompt, distinct deployment from judge-1's 120b, so the
# aggregator still deduplicates by model string and counts 3 judges).
- model_name: "judge-3-free"
  litellm_params:
    model: "groq/openai/gpt-oss-20b"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "judge-3-free-gpt-oss-20b"
    input_cost_per_token: 7.5e-8
    output_cost_per_token: 3e-7.00000000
- model_name: "judge-3-free"
  litellm_params:
    model: "groq/llama-3.1-8b-instant"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    input_cost_per_token: 5e-8
    output_cost_per_token: 8e-8

# util-fast-free — chat intent_classifier ("utility") + campaign util-fast key
- model_name: "util-fast-free"
  litellm_params:
    model: "groq/llama-3.1-8b-instant"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "util-fast-free-llama-3.1-8b"
    input_cost_per_token: 5e-8
    output_cost_per_token: 8e-8
- model_name: "util-fast-free"
  litellm_params:
    model: "groq/openai/gpt-oss-20b"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "util-fast-free-gpt-oss-20b"
    input_cost_per_token: 7.5e-8
    output_cost_per_token: 3e-7.00000000

# eval-model-free — intake "brand_truthfulness" + lightweight eval on free tier
- model_name: "eval-model-free"
  litellm_params:
    model: "groq/openai/gpt-oss-20b"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "eval-model-free-gpt-oss-20b"
    input_cost_per_token: 7.5e-8
    output_cost_per_token: 3e-7.00000000

# understanding-free — chat understanding_engine ("understanding")
- model_name: "understanding-free"
  litellm_params:
    model: "groq/llama-3.1-8b-instant"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "understanding-free-llama-3.1-8b"
    input_cost_per_token: 5e-8
    output_cost_per_token: 8e-8

# Understanding — merged intent classification + brief extraction (JSON-strong)
# Uses llama-3.1-8b-instant (temperature-0 structured JSON extraction); smaller
# model cuts time-to-first-token since this call must complete before streaming.
- model_name: "understanding"
  litellm_params:
    model: "groq/llama-3.1-8b-instant"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "understanding-llama-3.1-8b"
    input_cost_per_token: 5e-8
    output_cost_per_token: 8e-8
- model_name: "responder-chat-free"
  litellm_params:
    model: "groq/llama-3.1-8b-instant"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "responder-chat-free-llama-3.1-8b"
    input_cost_per_token: 5e-8
    output_cost_per_token: 8e-8

# brief-collector-free — chat brief_collector slot filling ("brief_collector")
- model_name: "brief-collector-free"
  litellm_params:
    model: "groq/llama-3.1-8b-instant"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "brief-collector-free-llama-3.1-8b"
    input_cost_per_token: 5e-8
    output_cost_per_token: 8e-8

# translation-primary-free — translation.py primary ("translation") on free tier.
# Pinned single-provider (Groq) for reproducible quality-gate scoring across retries.
- model_name: "translation-primary-free"
  litellm_params:
    model: "groq/llama-3.3-70b-versatile"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "translation-primary-free-llama-3.3-70b"
    input_cost_per_token: 5.9e-7
    output_cost_per_token: 7.9e-7

# translation-validator-free — translation.py fallback validator ("translation_validator")
# on free tier (back-translation / reference-translation fallback path only).
- model_name: "translation-validator-free"
  litellm_params:
    model: "groq/llama-3.1-8b-instant"
    api_key: "os.environ/GROQ_API_KEY"
  model_info:
    id: "translation-validator-free-llama-3.1-8b"
    input_cost_per_token: 5e-8
    output_cost_per_token: 8e-8

# ===================== PAID TIER — unchanged (moved here) =====================
# Selected when JUDGE_TIER=paid. Everything below is the pre-existing config,
# relocated into one section. No model updates were made in this change.

# Generation — premium (paid/enterprise tier). No Sonnet (2026-07-26, user
# request) — pooled across Claude Haiku + Gemini 3.5 Flash(-lite), with
# gpt-5.4-nano as a cheap OpenAI-family backup.
- model_name: "gen-premium"
  litellm_params:
    model: "anthropic/claude-haiku-4-5-20251001"
    api_key: "os.environ/ANTHROPIC_API_KEY"
  model_info: { id: "gen-premium-claude-haiku" }
- model_name: "gen-premium"
  litellm_params:
    # "Gemini 3.5 Flash" as requested — the only confirmed real ID in that
    # family is gemini-3.5-flash-lite (no plain non-lite "3.5 Flash" model
    # is currently verifiable); using -lite here. If a distinct plain
    # gemini-3.5-flash ID exists on your account, give me the exact string
    # and this is a one-line swap.
    model: "gemini/gemini-3.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "gen-premium-gemini-3.5-flash-lite" }
- model_name: "gen-premium"
  litellm_params:
    model: "openai/gpt-5.4-nano"
    api_key: "os.environ/OPENAI_API_KEY"
  model_info: { id: "gen-premium-gpt-5.4-nano" }

# Judges — pinned by family (never change family on fallback). Single
# deployment per judge (2026-07-26, user request), not pooled — reinforces
# the pinning philosophy rather than working against it, and keeps each
# judge's scoring behavior stable/predictable rather than varying by which
# pool member happened to answer.
#   judge-1 → Anthropic family, judge-2 → OpenAI family, judge-3 → Gemini
#   family. No Sonnet/heavy-tier models in any of the three.
# judge-2 swapped off gpt-5.4-mini to Gemini 3.5 Flash (2026-07-27, user
# request) — GPT-4o/gpt-5.4-mini was the sole factual_grounding rejector
# across 4 consecutive live campaigns (including one deliberately
# conservative prompt with no unverifiable claims at all), consistently
# overriding 2/3 judge consensus and mean scores of 0.79-0.86/1.0 via the
# any-critical-violation override. NOTE: this now means judge-2 and judge-3
# are both Gemini family, losing the cross-family diversity this panel was
# designed around (see comment above and judge-3 below) — an explicit,
# accepted tradeoff for now, not an oversight.
- model_name: "judge-1"
  litellm_params:
    model: "anthropic/claude-haiku-4-5-20251001"
    api_key: "os.environ/ANTHROPIC_API_KEY"

- model_name: "judge-2"
  litellm_params:
    model: "gemini/gemini-3.5-flash"
    api_key: "os.environ/GEMINI_API_KEY"

# judge-3 — see gen-premium's comment above re: "Gemini 3.5 Flash" mapping
# to gemini-3.5-flash-lite (only confirmed real ID in that family).
- model_name: "judge-3"
  litellm_params:
    model: "gemini/gemini-3.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"

# Utility — cheap model for classification, summarisation. Pooled across
# both mini tiers (2026-07-26, user request: no heavy models anywhere).
- model_name: "util-fast"
  litellm_params:
    model: "openai/gpt-5.4-mini"
    api_key: "os.environ/OPENAI_API_KEY"
  model_info: { id: "util-fast-gpt-5.4-mini" }
- model_name: "util-fast"
  litellm_params:
    model: "openai/gpt-4o-mini"
    api_key: "os.environ/OPENAI_API_KEY"
  model_info: { id: "util-fast-gpt-4o-mini" }

# Pooled across all 4 Flash quota buckets — this is only ever hit as a
# fallback (see router_settings below), so max headroom matters more than
# picking "the best" single Flash tier.
- model_name: "util-fast-gemini"
  litellm_params:
    model: "gemini/gemini-2.5-flash"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "util-fast-gemini-2.5-flash" }
- model_name: "util-fast-gemini"
  litellm_params:
    model: "gemini/gemini-2.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "util-fast-gemini-2.5-flash-lite" }
- model_name: "util-fast-gemini"
  litellm_params:
    model: "gemini/gemini-3.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "util-fast-gemini-3.5-flash-lite" }
- model_name: "util-fast-gemini"
  litellm_params:
    model: "gemini/gemini-3-flash"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "util-fast-gemini-3-flash" }

# Brief collector — dedicated extraction model for chat slot filling.
# Pooled across both mini tiers (2026-07-26, user request).
- model_name: "brief-collector"
  litellm_params:
    model: "openai/gpt-5.4-mini"
    api_key: "os.environ/OPENAI_API_KEY"
  model_info: { id: "brief-collector-gpt-5.4-mini" }
- model_name: "brief-collector"
  litellm_params:
    model: "openai/gpt-4o-mini"
    api_key: "os.environ/OPENAI_API_KEY"
  model_info: { id: "brief-collector-gpt-4o-mini" }

- model_name: "brief-collector-gemini"
  litellm_params:
    model: "gemini/gemini-2.5-flash"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "brief-collector-gemini-2.5-flash" }
- model_name: "brief-collector-gemini"
  litellm_params:
    model: "gemini/gemini-2.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "brief-collector-gemini-2.5-flash-lite" }
- model_name: "brief-collector-gemini"
  litellm_params:
    model: "gemini/gemini-3.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "brief-collector-gemini-3.5-flash-lite" }
- model_name: "brief-collector-gemini"
  litellm_params:
    model: "gemini/gemini-3-flash"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "brief-collector-gemini-3-flash" }

# Understanding — merged intent classification + brief extraction
# (JSON-strong). Pooled across both mini tiers (2026-07-26, user request).
- model_name: "understanding"
  litellm_params:
    model: "openai/gpt-5.4-mini"
    api_key: "os.environ/OPENAI_API_KEY"
  model_info: { id: "understanding-gpt-5.4-mini" }
- model_name: "understanding"
  litellm_params:
    model: "openai/gpt-4o-mini"
    api_key: "os.environ/OPENAI_API_KEY"
  model_info: { id: "understanding-gpt-4o-mini" }

- model_name: "understanding-gemini"
  litellm_params:
    model: "gemini/gemini-2.5-flash"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "understanding-gemini-2.5-flash" }
- model_name: "understanding-gemini"
  litellm_params:
    model: "gemini/gemini-2.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "understanding-gemini-2.5-flash-lite" }
- model_name: "understanding-gemini"
  litellm_params:
    model: "gemini/gemini-3.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "understanding-gemini-3.5-flash-lite" }
- model_name: "understanding-gemini"
  litellm_params:
    model: "gemini/gemini-3-flash"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "understanding-gemini-3-flash" }

# Responder — conversational turn generation. Pooled across both mini
# tiers (2026-07-26, user request: no heavy models anywhere).
- model_name: "responder-chat"
  litellm_params:
    model: "openai/gpt-5.4-mini"
    api_key: "os.environ/OPENAI_API_KEY"
  model_info: { id: "responder-chat-gpt-5.4-mini" }
- model_name: "responder-chat"
  litellm_params:
    model: "openai/gpt-4o-mini"
    api_key: "os.environ/OPENAI_API_KEY"
  model_info: { id: "responder-chat-gpt-4o-mini" }

- model_name: "responder-chat-gemini"
  litellm_params:
    model: "gemini/gemini-2.5-flash"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "responder-chat-gemini-2.5-flash" }
- model_name: "responder-chat-gemini"
  litellm_params:
    model: "gemini/gemini-2.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "responder-chat-gemini-2.5-flash-lite" }
- model_name: "responder-chat-gemini"
  litellm_params:
    model: "gemini/gemini-3.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "responder-chat-gemini-3.5-flash-lite" }
- model_name: "responder-chat-gemini"
  litellm_params:
    model: "gemini/gemini-3-flash"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "responder-chat-gemini-3-flash" }

# Evaluation
- model_name: "eval-model"
  litellm_params:
    model: "anthropic/claude-haiku-4-5"
    api_key: "os.environ/ANTHROPIC_API_KEY"

# Translation — pinned single-provider; never pooled (consistent model
# family required for reproducible quality-gate scoring across retries —
# unlike the pooled aliases above, rotating providers mid-retry-loop here
# would make the quality gate's pass/fail inconsistent across attempts).
# Switched to Claude Sonnet (2026-07-27, user request) after gemini-2.5-flash
# was confirmed live (campaign 019fa487) to fall into a repetition-
# degeneration loop on long marketing copy — e.g. "bleu"/"bleue" repeated
# 20+ times in one French email, verified via Helsinki-NLP's faithful
# back-translation round-trip (see next_tasks.md, translation quality
# investigation). Previously ran gemini-2.5-pro -> gemini-2.5-flash
# (2026-07-26, no-Pro-anywhere directive) before this.
- model_name: "translation-primary"
  litellm_params:
    model: "anthropic/claude-sonnet-4-6"
    api_key: "os.environ/ANTHROPIC_API_KEY"

# Translation validator — reverse-translation / reference-translation
# fallback paths ONLY (pipeline/agents/translation.py: _back_translate and
# the reference-translation fallback in _run_translation_gate). Primary path
# for both is now Helsinki-NLP (HF Inference Providers, confirmed live) —
# this alias only fires if that HF call itself errors, not on every retry
# attempt, so — unlike translation-primary above — it's safe to pool across
# providers (2026-07-26, user request; gpt-4o swapped for gpt-5.4-mini here
# per the "no heavy models" directive from items 26/27).
- model_name: "translation-validator"
  litellm_params:
    model: "gemini/gemini-2.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "translation-validator-gemini-2.5-flash-lite" }
- model_name: "translation-validator"
  litellm_params:
    model: "gemini/gemini-3.5-flash-lite"
    api_key: "os.environ/GEMINI_API_KEY"
  model_info: { id: "translation-validator-gemini-3.5-flash-lite" }
- model_name: "translation-validator"
  litellm_params:
    model: "openai/gpt-5.4-mini"
    api_key: "os.environ/OPENAI_API_KEY"
  model_info: { id: "translation-validator-gpt-5.4-mini" }

# ============================== SHARED / SYSTEM ==============================
# Embeddings — tier-independent. NOTE: RAG currently uses LOCAL
# sentence-transformers (backend/services/rag/embeddings.py), so this alias is
# effectively unused today; kept for a provider-routed embedding path.
- model_name: "embedding"
  litellm_params:
    model: "openai/text-embedding-3-small"
    api_key: "os.environ/OPENAI_API_KEY"

router_settings:
  fallbacks:
  - { "responder-chat": [ "responder-chat-gemini" ] }
  - { "util-fast": [ "util-fast-gemini" ] }
  - { "brief-collector": [ "brief-collector-gemini" ] }
  - { "understanding": [ "understanding-gemini" ] }
  retry_policy:
    BadRequestError: { num_retries: 0 }
    RateLimitError: { num_retries: 3, retry_after: 60 }
    ServiceUnavailableError: { num_retries: 3 }

litellm_settings:
  cache: true
  cache_params:
    type: redis
    host: redis
    port: 6379
    ttl: 3600
  drop_params: true
  max_budget: 10.0
  budget_duration: "1d"
